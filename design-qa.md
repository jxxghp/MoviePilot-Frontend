# 站点资源浏览弹窗视觉 QA

日期：2026-09-13

## 参考与实现

- 参考图：`/var/folders/bq/sh57s52x40gf5tj5h4tb33_40000gn/T/codex-clipboard-42be4c2e-61ae-4e35-8515-3f9b72da6f47.png`（移动端）
- 参考图：`/var/folders/bq/sh57s52x40gf5tj5h4tb33_40000gn/T/codex-clipboard-468283b7-ac2a-44d0-b096-fa7335e24e44.png`（桌面端）
- 实现：`src/components/dialog/SiteResourceDialog.vue`

## 浏览器证据

- 1280×960、玻璃紫色主题：筛选区、资源横向卡片、四列指标和右上角更多操作均可见；滚动接近底部后请求页码 1，资源摘要从 100 增长到 200。
- 360×874、玻璃紫色主题：工具栏完整显示“浏览 - 音乐乌托邦”；筛选区压缩至约 231px，滚动区从 y=288 开始，排序原生箭头和主题边界可见，整卡无横向溢出。
- 390×844、玻璃紫色主题：筛选控件纵向排列，搜索文字距边界约 12px，资源卡片无横向溢出，标题和描述使用两行截断，指标区保持四列紧凑布局。
- 390×844、玻璃紫色主题：资源区上滑后筛选区收起为搜索图标，点击图标恢复输入区；排序入口仅保留排序图标和当前选项，宽度按内容自适应且不显示下拉箭头或外层边框。
- 390×844、玻璃紫色主题：资源分类多选 3 项时，右侧选项自动换行并撑开输入行高度；标签与输入区按左 4、右 6 的比例布局，搜索按钮和资源列表不被遮挡。
- 1280×720、玻璃紫色主题：桌面筛选横排、排序控件和四列指标保持完整；首张卡片在滚动区顶部正常显示。
- 1280×720、玻璃紫色主题：搜索、资源分类和搜索按钮三列之间恢复独立间隔，控件宽度比例保持 5/5/2。
- 390×844、浅色主题：白色主题表面、紫色主按钮、语义标签、排序边界/箭头和指标分隔线随全局主题生效。
- 1280×960、透明主题：透明表面和主题提供的磨砂参数生效，桌面横向资源卡仍保持完整布局。
- 资源卡整卡点击实测打开“确认下载”对话框；点击更多操作不会触发整卡下载。
- 主题恢复：验收结束后已恢复玻璃紫色主题。

## 对照结论

- 弹窗外层 `VDialog`、`VCard` 未改动；内部标题区使用通用 `VCardItem`，含资源浏览图标、主标题“浏览”、站点名称副标题和通用关闭按钮，不再使用独立主色工具栏底色。
- 输入框、选择器、按钮、标签、菜单、进度条和状态提示均使用 Vuetify 原生组件。
- 卡片背景、边框、圆角、阴影、悬停状态和磨砂效果均引用全局主题 token；组件内没有新增固定渐变、固定透明度或固定 blur 值。
- 桌面端和移动端共用 `ProgressiveCardGrid`、弹窗内部滚动容器和分页预取逻辑；资源页按滚动阈值渐进加载并去重。
- 当前 `TorrentInfo` 数据契约没有封面字段，因此没有伪造海报资源；保留参考图中的标题、描述、标签和指标信息层级。

## 结果

passed
